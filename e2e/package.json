{
  "name": "icoip-e2e",
  "private": true,
  "scripts": {
    "smoke":      "playwright test --config=playwright.config.js --project=smoke",
    "auth":       "playwright test --config=playwright.config.js --project=auth",
    "access":     "playwright test --config=playwright.config.js --project=access",
    "workflow":   "playwright test --config=playwright.config.js --project=workflow",
    "regression": "playwright test --config=playwright.config.js --project=regression",
    "critical":   "playwright test --config=playwright.config.js --grep @critical",
    "all":        "playwright test --config=playwright.config.js",
    "report":     "playwright show-report reports"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
